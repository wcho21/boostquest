<!DOCTYPE html>
<html>
  <head>
    <%- include('includes/head', { pageStylesheet: 'day' }); %>
  </head>
  <body>
    <%- include('includes/header'); %>

    <main id="layout-day-page">
      <%- include('../challenges/description/' + day + 1); %>

      <% if (!signedIn) { %>
        <p>더 진행하기 위해, <a href='/signin'>로그인</a>이 필요합니다.</p>
      <% } else { %>

        <% if (firstProblemSolved) { %>
          <%- include('../challenges/description/' + day + 2); %>
        <% } %>

        <% if (secondProblemSolved) { %>
          <h2>문제를 해결했습니다!</h2>

          <%- include('../challenges/description/' + day + 9); %>

          <p><a href='/'>이전</a>으로 돌아가서 다른 문제를 시도해볼 수 있습니다.</p>
          <p>문제는 이미 해결했지만 <a href='/day/<%= day %>/input' target='_blank'>입력 파일</a>은 볼 수 있습니다.</p>
        <% } else { %>
          <p><a href='/day/<%= day %>/input' target='_blank'>입력 파일</a>을 받아서 문제를 해결해주세요.</p>
          <form action='/answer/<%= day %>' method='post'>
            <input type='text' id='answer' name='answer' placeholder='여기에 답을 입력하세요'></input>
            <input type='submit' id='submit' value='제출'></input>
          </form>
        <% } %>
      <% } %>
    </main>
  </body>
</html>
